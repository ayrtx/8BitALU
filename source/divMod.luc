module compare8 (
  input a[8],
  input b[8],
  input alufn[6],
  output out[8]
  ) {

  always {
  
    case(alufn[2:1]){
      b010: // division
        out = a / b;
      b011: // modulo
        out = a - (a / b*b);
    }
  }
}
