module adder (
  input a[8],
  input b[8],
  input alufn[6],
  output sum[8]

    
  ) { 
  
  always {
    sum = 0;
      case(alufn[1:0]){
        b00:
          sum = a + b; //summation of a and b
          
        b01:
          sum = a - b;  //subtraction of a and b
    }  
  }
}
